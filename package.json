{
  "name": "jarlbot",
  "version": "1.0.0",
  "description": "Discord bot and portable launcher for EVA community administration.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "eva-refresh": "node scripts/eva-refresh.js",
    "eva-refresh:full": "node scripts/eva-refresh.js --full",
    "eva-refresh:reset": "node scripts/eva-refresh.js --reset --full",
    "eva-refresh:daemon": "node scripts/eva-refresh.js --daemon",
    "eva-public-refresh": "node scripts/eva-public-refresh.js",
    "eva-public-status": "node scripts/eva-public-refresh.js --status",
    "launcher": "node scripts/launcher.js",
    "export-portable": "node scripts/export-portable.js",
    "deploy-commands": "node deploy-commands.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "discord.js": "^14.26.4",
    "dotenv": "^17.4.2",
    "node-schedule": "^2.1.1"
  }
}
